[ghost:children]
blog_name

[ghost:vars]
ansible_user=ec2-user

[blog_name]
blogname.com

[blog_name:vars]
ansible_ssh_private_key_file=~/.ssh/blogname.pem
domain_name=blogname.com
letsencrypt_email=user@whatever.com
mailgun_smtp_username=(obtained via the mailgun 'domains' page)
mailgun_smtp_password=(obtained via the mailgun 'domains' page)
